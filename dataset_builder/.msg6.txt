Keep one ready dataset per app: 13 apps, 13 entries, no repeats

Every app now appears exactly once, with a single APK, a single smoke result and a
single submodule. All 13 produce a clean coverage report with no classes
excluded.

    MaterialFBook      19.24%      nextcloud        8.63%
    geohashdroid       17.78%      commons          7.57%
    StreetComplete     15.20%      newpipe          7.57%
    FirefoxLite        13.72%      AnkiDroid        4.11%
    AmazeFileManager   13.08%      Wikipedia        3.41%
    Omni-Notes         10.58%      Twire            2.67%
                                   openlauncher     2.35%

Removed:

  5 apps whose reports exclude mismatched classes rather than being clean:
    Vinyl-Music-Player, APhotoManager, Open-Food-Facts,
    open-event-attendee-android, Fedilab
  4 duplicate APK entries inside surviving apps, so no app is represented twice:
    AnkiDroid, commons, geohashdroid and openlauncher each had a published APK
    alongside the rebuilt one
  11 smoke result directories belonging to anything removed
  5 submodule registrations for the removed apps

Where an app had both, the rebuilt APK is kept: its classes are byte-for-byte the
ones declared in class_files.json, so it cannot drift from them. MaterialFBook is
the exception, having only ever had a published APK.

Summary tables rewritten to match: smoke_summary.csv 42 rows to 13,
probe_summary_all.csv 24 to 13. The manifest reports 13 apps, 13 entries, 13
valid.

Nothing is lost permanently. Git history keeps every removed tree, and
dataset_builder/ still describes all 44 original subjects.
